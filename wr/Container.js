
wr.Container = function(collection, renderChild) {
  this.collection = collection;
  this.renderChild = renderChild;
};


wr.Container.prototype.create = function() {
};


wr.Container.prototype.insert = function(node, before) {
};
